---
galaxy_info:
  namespace: vnode
  role_name: ypserver
  author: Rogier Krieger
  description: Install and configure a YP/NIS server
  company: vNode.net
  issue_tracker_url: https://github.com/vnode/ansible-role-ypserver/issues/
  license: MIT
  min_ansible_version: '2.9'
  # If this a Container Enabled role, provide the minimum Ansible
  # Container version.
  # min_ansible_container_version:

  # To view available platforms and versions (or releases), visit:
  # https://galaxy.ansible.com/api/v1/platforms/
  platforms:
  - name: OpenBSD
    versions:
    - all

  galaxy_tags:
    - "system"
    - "yp"
    - "ypserver"
    - "nis"

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above, if you add dependencies to this list.
